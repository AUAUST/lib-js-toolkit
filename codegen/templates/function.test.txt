import { {{function}} } from "@auaust/toolkit";
import { describe, expect, expectTypeOf, test } from "vitest";

describe("{{function}}()", () => {
  test("placeholder", () => {
    expect({{function}}).not.toThrow();
  });
});
